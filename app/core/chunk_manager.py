# from typing import Iterator

# CHUNK_SIZE = 4 * 1024 * 1024

# def chunk_data(data: bytes) -> Iterator[bytes]:

#     for i in range(0, len(data), CHUNK_SIZE):
#         yield data[i : i + CHUNK_SIZE]
